"""Pytest configuration / shared fixtures (placeholder).

Phase 4 skeleton: no fixtures are defined yet. Behavioral fixtures (MockProvider,
MockSearchTool, sample configs/messages) are added in Phase 5 alongside the first
real tests. This file exists so the tests package is importable and ready.
"""
