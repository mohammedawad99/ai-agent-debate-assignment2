"""CLI entry point — placeholder (Phase 4 skeleton).

The real CLI will be a thin layer over the SDK (see docs/ARCHITECTURE.md §3/§6).
No CLI behavior is implemented yet; this only satisfies the console-script entry
point declared in pyproject.toml.
"""

from __future__ import annotations


def main() -> None:
    """Placeholder entry point. Not implemented in the skeleton phase."""
    raise NotImplementedError(
        "agent-debate CLI is not implemented yet (Phase 4 skeleton). "
        "See docs/TODO.md for the implementation phases."
    )
