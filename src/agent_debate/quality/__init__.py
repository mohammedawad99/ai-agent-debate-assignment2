"""Quality / scoring layer.

Future home of ScoreCalculator and TieBreaker: pure, deterministic scoring (0-5,
equal weight, sum) and the configured tie-break cascade (no LLM call). See
docs/SCORING_AND_VALIDATION.md §7-9. No implementation yet (Phase 4 skeleton).
"""
