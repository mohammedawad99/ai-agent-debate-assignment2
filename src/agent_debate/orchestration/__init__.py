"""Orchestration layer.

Future home of DebateRunner / DebateSession: the turn state machine,
parent-mediated routing, the validate->regenerate loop, and the scoring trigger
(ARCHITECTURE §6/§7). No implementation yet (Phase 4 skeleton).
"""
