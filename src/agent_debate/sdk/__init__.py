"""SDK / application service layer.

The single public surface the CLI uses. Constructs and injects dependencies from
config; exposes "run a debate" services. All business logic is reachable here
(ARCHITECTURE DR-2). No implementation yet (Phase 4 skeleton).
"""
