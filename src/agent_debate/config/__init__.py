"""Config layer.

Future home of ConfigLoader + typed config objects. Loads/validates the JSON
templates under the top-level `config/` directory; the only source of tunable
values (ARCHITECTURE DR-7). No implementation yet (Phase 4 skeleton).
"""
